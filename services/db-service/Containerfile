# Initializes PostgreSQL with project schema and seed data.
FROM docker.io/library/postgres:14-alpine

# Place schema/data init SQL in official entrypoint initdb.d (runs at container first start)
COPY init_pg.sql /docker-entrypoint-initdb.d/
